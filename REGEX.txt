\d{3}\.?                      	? = OPCIONAL - QUER DIZER QUE . É OPCIONAL, TBM PODE SER USADO {0,1}

[-.]							DENTRO DOS COLCHETES DEFINA UMA CLASSE DE CARACTERES, VALOR LITERAL DOS CARACTERES, SEM A NECESSIDADE DE \

? = ZERO OU UMA VEZ
* = ZERO OU MAIS VEZES
+ = UMA OU MAIS VEZES


OPERADORES: +-*/%= == != > < >= <= && || " *= /= += -= & | ++ -- ? :
DELIMITADORES: ( ) ; [ ] { } " ' . , // /* */
PALAVRAS RESERVADAS: public static void boolean private protected true false new for while do if else
break continue final return int float char class extends implements package import abstract switch case double long
default interface try catch this null rip throw super

